# Template for all projects. This is a work in progress.

Git clone, cd in, and run intall.sh

```bash
git clone https://github.com/BespokeReality/Server-Template
cd Server-Template
sudo ./install.sh
```


